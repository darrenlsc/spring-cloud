server:
  port: 8762
spring:
  application:
    name: eureka
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/
    fetch-registry: false
    register-with-eureka: false